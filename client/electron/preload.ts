/**
 * Deliberately empty preload boundary.
 *
 * Hamlet renderer code remains browser-only and calls the Hamlet server directly
 * over HTTP/SSE/LiveKit. Do not expose ipcRenderer or broad system APIs here.
 */
export {};
